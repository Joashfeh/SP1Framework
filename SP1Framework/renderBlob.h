#pragma once
#include "Framework/console.h"
#include "colours.h"
void renderblob(Console& g_Console, int x, int y, bool spinny);
void renderEye(Console& g_Console, int x, int y);
void renderMouth(Console& g_Console, int x, int y, int size, int start);
